# Agent task resources

Use `task-template.md` to define implementation work and `review-template.md`
for read-only reviews. The authoritative instructions are `../AGENTS.md`; the
detailed guidance index is `../docs/agent-guidelines/README.md`.
UI tasks and reviews populate visual-reference fields from
`../design-references/manifest.yaml`.
