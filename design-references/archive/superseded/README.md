# Superseded references

Historically useful replaced images are retained here as non-authoritative
records. Every image requires a `superseded` manifest entry linked to its
replacement and must not guide new implementation.
